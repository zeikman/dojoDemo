define([
    'app/js/base',
    'ready!'
], function(
    // base
) {
    var app = {
        init: function() {
            // window.alert('hello');
        }
    };

    app.init();

    return app;
});